Ideas:

neofetch config  import
.zshrc   profile import
.git	 diff	 detect/sync

$ENV	 vars	 git tags

vim	 config	 setup
zsh	 config  import
omz	 config  import
powerlvl config	 import

node	 config  import
nvm	 config  import
aws	 config  import
